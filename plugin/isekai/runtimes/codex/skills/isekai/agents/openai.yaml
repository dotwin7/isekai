interface:
  display_name: "ISEKAI"
  short_description: "Run ISEKAI only through an explicit $isekai-agent-plugin:isekai command"
  default_prompt: "$isekai-agent-plugin:isekai status"

policy:
  allow_implicit_invocation: false
